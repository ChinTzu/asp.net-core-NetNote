@model NetNote.Models.Note

<div class="row">
    @if (Model == null)
    {
        <form method="post" asp-action="Detail">
            <div class="form-group">
                <label asp-for="Password"></label>
                <input asp-for="Password" type="password" placeholder="Please enter the password" class="form-control" />
            </div>
            <p>
                <button type="submit" class="btn text-center">View</button>
            </p>
        </form>
    }
    else
    {
        <h3> @Model.Title</h3>
        <h5>Type：@Model.Type.Name</h5>
        <div class="content">@Model.Content</div>
        if (!string.IsNullOrEmpty(Model.Attachment))
        {
            <a href="@Model.Attachment">Download</a>
        }
    }
</div>
